.hd-popup {
  padding-bottom: env(safe-area-inset-bottom);
  overflow-y: scroll;
}
.title {
  display: flex;
  align-items: center;
  padding-left: 12px;
  height: 44px;
  color: #333;
  font-size: 16px;
  font-weight: bold;
  line-height: 1;
}
