import VideoService from "../../utils/videoService";

const videoService = new VideoService();

Component({
  properties: {
    videoId: Number,
    isSticky: Boolean,
    isPrivate: Boolean,
  },

  methods: {
    toggleSticky() {
      
    },

    delete() {
      
    },

    togglePrivate() {
    },
  },
});
