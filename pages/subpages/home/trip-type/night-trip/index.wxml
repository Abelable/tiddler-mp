<view class="back-icon-wrap" style="top: {{statusBarHeight + 5}}px">
  <back-icon />
</view>

<view class="trip-list" style="margin-top: {{statusBarHeight + 320}}px;">
  <trip-item wx:for="{{scenicList}}" wx:key="index" item="{{item}}" />
</view>
