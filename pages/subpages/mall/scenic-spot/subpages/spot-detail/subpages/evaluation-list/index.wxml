<view class="container">
  <evaluation-item wx:for="{{evaluationList}}" wx:key="index" item="{{item}}" />

  <empty-illus wx:if="{{finished && !evaluationList.length}}" desc="暂无评价" height="520rpx" />
  <view class="no-more-tips" wx:if="{{finished && evaluationList.length}}">～没有更多了～</view>
</view>
