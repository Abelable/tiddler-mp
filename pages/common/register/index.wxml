<view class="login-wrap">
  <image class="bg" mode="widthFix" src="./images/bg.jpeg" />
  <view class="back-icon-wrap" style="top: {{statusBarHeight + 5}}px">
    <i-back-icon />
  </view>

  <view class="content-wrap" style="margin-top: {{statusBarHeight + 100}}px">
    <view class="title">Hi,旅行者</view>
    <view class="sub-title">欢迎来到千岛湖！</view>
    <view class="tips">
      <view>旅途的意义也许不在于目的地，​</view>
      <view>而是沿途的风景​</view>
    </view>
    <button class="register-btn" open-type="getPhoneNumber" bindgetphonenumber="getPhoneNumber">
      <view>开启旅途</view>
      <image class="arrow" src="./images/arrow.png" />
    </button>
    <view class="agreement-tips">
      <text>开启旅途即表明您同意</text>
      <text style="color: #0095fa;" ontap="serviceAgreement">《用户服务协议》</text>
    </view>
  </view>
</view>

<view class="modal-mask" wx:if="{{showAuthModal}}" bindtap="hideModal" />
<view class="modal-dialog" wx:if="{{showAuthModal}}">
  <view class="modal-title">
    <image style="width: 290rpx; height: 184rpx;" src="https://img.ubo.vip/mp/login/login-info.png" />
  </view>
  <view class="modal-content auth-dialog-content">
    <view>申请获得您的公开信息</view>
    <view>(包含头像，昵称等)</view>
  </view>
  <view class="modal-footer">
    <button class="btn-confirm" bindtap="getUserProfile">确认授权</button>
  </view>
</view>