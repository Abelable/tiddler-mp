Component({
  options: {
    addGlobalClass: true,
  },

  properties: {
    list: Array,
  },

  methods: {
    previewImage(e) {
      // const { current } = e.currentTarget.dataset;
      // const urls = this.properties.list;
      // wx.previewImage({ current, urls });
    },
  },
});
