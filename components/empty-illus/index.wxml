<view class="empty">
  <image class="illus" src="https://img.ubo.vip/tiddler/empty.png" />
  <view>{{registerBtnVisible ? '您还未登录' : desc}}</view>
  <button class="register-btn" wx:if="{{registerBtnVisible}}">点击登录</button>
</view>
