<view class="empty" style="height: {{height}};">
  <image class="illus" src="https://img.ubo.vip/tiddler/temp/empty.png" />
  <view>{{registerBtnVisible ? '您还未登录' : desc}}</view>
  <button class="register-btn" wx:if="{{registerBtnVisible}}" bindtap="register">点击登录</button>
</view>
